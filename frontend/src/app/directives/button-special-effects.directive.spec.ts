import { ButtonSpecialEffectsDirective } from './button-special-effects.directive';

describe('ButtonSpecialEffectsDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonSpecialEffectsDirective();
    expect(directive).toBeTruthy();
  });
});
