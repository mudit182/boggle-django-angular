import { SelectionListItemHoverDirective } from './selection-list-item-hover.directive';

describe('SelectionListItemHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectionListItemHoverDirective();
    expect(directive).toBeTruthy();
  });
});
