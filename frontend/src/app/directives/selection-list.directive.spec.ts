import { SelectionListDirective } from './selection-list.directive';

describe('SelectionListDirective', () => {
  it('should create an instance', () => {
    // const directive = new SelectionListDirective();
    // expect(directive).toBeTruthy();
  });
});
